version: '3.8'

services:
    frontend:
        container_name: secure_otp-frontend
        build:
            context: .
            dockerfile: Dockerfile
        environment:
            - TFA_BACKEND_URL=${BACKEND_URL}
        ports:
            - ${EXPOSE_PORT}:4200
